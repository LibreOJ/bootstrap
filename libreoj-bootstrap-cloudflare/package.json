{
  "private": true,
  "name": "libreoj-bootstrap-cloudflare",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "Menci <huanghaorui301@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build:code": "esbuild src/index.ts --bundle --outfile=dist/index.js",
    "build:config": "node --experimental-json-modules config.mjs",
    "build": "yarn build:code && yarn build:config",
    "dev": "yarn build && wrangler dev",
    "pre-deploy": "yarn build"
  },
  "devDependencies": {
    "@iarna/toml": "^2.2.5",
    "esbuild": "^0.12.24",
    "libreoj-bootstrap-core": "*"
  }
}
